# Refactoring page

You need to refactor the pages in the `app` folder.

Do not touch the `api` folder.

## Order

- [x] The contents of `/dashboard/page.tsx` need to be converted into a separate `youtube` component and connected to the main page `/page.tsx`.
- [x] delete `/dashboard/page.tsx`
- [x] The contents of `/youtube-update/page.tsx` need to be converted into a separate component `youtube-update`.
- [x] Create a protected route `/admin/page.tsx`. The next step is to add authorization for this route (but not now). Create `/admin/page.tsx` and connect the `youtube-update` component to it.
