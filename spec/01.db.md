# Database Feature

## Description

Setup DB and ORM

## Requirements

## System components

Next.js (App Router)
Server-side components (RSC) for reading data
Route Handlers (app/api/\*) for background tasks and webhook-like actions
Data collection service (ingestion layer)
YouTube API access logic
Data normalization and validation
PostgreSQL in Docker
ORM / SQL layer

## Application structure

```
app/
  layout.tsx
  page.tsx

  dashboard/
    page.tsx        // server component, reads from database

  api/
    ingest/
      youtube/
        route.ts    // manual launch or webhook

lib/
  db/
    index.ts        // connection to Postgres
    schema.ts
  youtube/
    client.ts       // YouTube API client
    mapper.ts       // API mapping → domain model
  jobs/
    youtube.ts      // periodic collection logic

prisma/ (or drizzle/)
docker/
```

## Working with YouTube API

Separating layers
Important: Do not call YouTube API directly from RSC.

The correct approach:

YouTube API is called:

either from a background job
or from API Route (route.ts)

RSC only reads data from the database.

#### lib/youtube/client.ts

Responsibilities:

Authentication (API key / OAuth if necessary)
Retry / rate limit

Raw requests

#### lib/youtube/mapper.ts

Converts:

YouTube API response
→ Channel, Video, Stats (domain model)

## Сбор данных (ingestion)

Варианты запуска - Отдельный ingestion-процесс

Поток данных
Cron / Job
→ YouTube API
→ normalize
→ save to Postgres

Все операции записи должны быть идемпотентны:

upsert по youtube_video_id

## Database (Postgres)

Docker

```
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    volumes:
      - pgdata:/var/lib/postgresql/data
```

## Server Components (RSC)

Principles:

- SELECT only
- No external APIs
- Caching via cache() / revalidateTag

Example:

```
export const revalidate = 300;

export default async function Dashboard() {
  const videos = await db.query.videos.findMany({
    with: { stats: true }
  });

  return <DashboardView videos={videos} />;
}
```

## API Routes (Route Handlers)

Используются для:

- ручного запуска ingestion
- webhook / admin-действий

Example:

```
POST /api/ingest/youtube
```

Required:

auth (secret token)
rate limit
no UI logic

## Caching and updating

Levels:

- Database — source of truth
- Next.js cache (revalidate, revalidateTag)

After ingestion:

- revalidateTag(“videos”)

## Security

YouTube API keys:

- server-side only
- via env
- Close ingestion endpoints
- Limit YouTube API quota

## Drizzle ORM

Install Drizzle ORM
Connect to Postgres

### Подключение к Postgres (Drizzle)

lib/db/index.ts

```
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
});

export const db = drizzle(pool);

```

Important:

- singleton pool
- no connect/disconnect in RSC

## Domain model

What really needs to be stored

Video:

- youtube_id
- title
- description
- image (thumbnail)
- link
- channels

2. Final database schema
   Tables
   channels
   videos

Only two tables.

## Channels schema

```
// lib/db/schema/channels.ts
import { pgTable, uuid, text, timestamp } from ‘drizzle-orm/pg-core’;

export const channels = pgTable(‘channels’, {
  id: uuid(‘id’).defaultRandom().primaryKey(),

  youtubeId: text(‘youtube_id’).notNull().unique(),

  title: text(‘title’).notNull(),

  createdAt: timestamp(‘created_at’, { withTimezone: true })
    .defaultNow()
    .notNull(),
});
```

## videos schema (simplified and clean)

// lib/db/schema/videos.ts

```
import {
  pgTable,
  uuid,
  text,
  timestamp,
} from ‘drizzle-orm/pg-core’;
import { channels } from ‘./channels’;

export const videos = pgTable(‘videos’, {
  id: uuid(‘id’).defaultRandom().primaryKey(),

  youtubeId: text(‘youtube_id’).notNull().unique(),

  channelId: uuid(‘channel_id’)
    .references(() => channels.id, { onDelete: ‘cascade’ }),

  title: text(‘title’).notNull(),
  description: text(‘description’),

  imageUrl: text(‘image_url’).notNull(),

  link: text(‘link’).notNull(),

  publishedAt: timestamp(‘published_at’, {
    withTimezone: true,
  }),

  createdAt: timestamp(‘created_at’, { withTimezone: true })
    .defaultNow()
    .notNull(),
});
```
